2026-02-21 06:46 BRT

Status
- Atendimento: backend concluído (filtros, mensagens, last_message robusto) e correção de filtro de canal.
- Frontend: rotas em inglês (/ → /dashboard, Atendimento → /inbox) + sidebar com altura fixa e menu scrollável.
- Canais: MVP implementado e mergeado no main; runtime ainda precisa deploy/restart para refletir POST /channels com is_default.

Riscos
- Runtime desatualizado pode mascarar bugs e atrasar validação do módulo de Canais.
- Falta de validação pós-deploy pode gerar regressões não detectadas.

Próximos passos
- Executar deploy/restart do runtime e validar POST /channels com is_default.
- Smoke tests rápidos em Atendimento + Canais após deploy.
- Consolidar feedback e abrir ajustes prioritários.

---

2026-02-21 10:46 BRT

Status
- Sem mudança confirmada desde o último registro; backlog focado em deploy/restart do runtime para refletir POST /channels com is_default.
- Atendimento e rotas do frontend continuam em estágio pós-merge aguardando validação em runtime.

Riscos
- Deploy pendente mantém inconsistência entre código e ambiente, atrasando feedback real.
- Falta de validação end-to-end pode esconder regressões em canais/atendimento.

Próximos passos
- Agendar e executar deploy/restart do runtime o quanto antes.
- Validar end-to-end: POST /channels (is_default) + fluxos de Atendimento.
- Registrar achados e abrir correções prioritárias.
