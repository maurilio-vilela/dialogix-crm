2026-02-21 02:46 (America/Sao_Paulo)

Status
- Backend de Atendimento concluído (filtros, mensagens, last_message robusto; correção no filtro de canal evitando 500).
- Frontend com rotas em inglês (/ → /dashboard, Atendimento → /inbox) e sidebar com altura fixa + menu scrollável.
- Módulo de Canais MVP implementado e já no main; runtime ainda precisa deploy/restart para refletir POST /channels com is_default.

Riscos
- Deploy/restart pendente pode mascarar regressões e bloquear validação do endpoint POST /channels (is_default).
- Mudanças de rotas podem impactar links/ bookmarks e testes E2E se não atualizados.

Próximos passos
- Executar deploy/restart do runtime para aplicar o endpoint POST /channels com is_default.
- Validar rotas atualizadas no frontend e ajustar testes/links legados.
- Checar smoke tests dos filtros de Atendimento pós-deploy.
