# Relatório do Oráculo - 2026-02-20 14:46

## Status
- MVP base (Auth + Contatos) segue funcional no código e versionado.
- Frontend do módulo **Atendimento** está em desenvolvimento local com integração de WebSocket e fluxo de mensagens (listagem de conversas, mensagens, envio e UI de detalhes).
- Existe checklist de QA para o módulo Atendimento documentado em `notes/scribe/attendance-qa.md`.
- Persistem mudanças locais não commitadas (frontend e notas), indicando trabalho em andamento e ainda não entregue no repositório.

## Riscos
1. **Deploy bloqueado (crítico):** persistem indícios de falha de SSH/Git no servidor ("Permission denied publickey"), o que impede sincronizar produção com o código atualizado.
2. **Gap entre local e produção:** novas evoluções (Atendimento/WebSocket/QA) ainda não estão consolidadas em commit, aumentando risco de perda de contexto/rollback difícil.
3. **Dependência de WebSocket:** sem validação em ambiente real, o realtime pode falhar por roteamento/infra (Traefik) e degradar a experiência.

## Próximos Passos
1. **Resolver acesso SSH do servidor** e validar `git pull` (prioridade absoluta para destravar deploy).
2. **Consolidar mudanças locais**: revisar o módulo Atendimento, ajustar pendências e gerar commit no repositório.
3. **Testes funcionais** usando o checklist de QA do Atendimento para validar fluxos críticos e edge cases.
4. **Deploy controlado** após a consolidação, com validação do WebSocket em produção.
